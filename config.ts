// This file is for client-side configuration that does not involve secrets.
// For this project, it is currently not used, but is kept for future scalability.

export const IS_DEVELOPMENT = process.env.NODE_ENV === 'development';
